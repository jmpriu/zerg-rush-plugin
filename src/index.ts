import { ZergRush } from './ZergRush'


export const calc = (x: number, y: number): number => x * y
export const zergRush = new ZergRush(10)
calc(2, 2)
const a = document.getElementById('a');
if (a !== null) {
    a.style.accentColor = 'blue'
}
console.log("Hi from the console")
